{
  "name": "@fig/fig-api-proto",
  "version": "1.0.0",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "./build-ts.sh",
    "lint": "buf lint && (buf format --exit-code > /dev/null || (echo 'Run `buf format -w`' && exit 1))",
    "lint:fix": "buf format -w",
    "precommit": "exit 0"
  },
  "devDependencies": {
    "ts-proto": "~1.157.1"
  }
}
