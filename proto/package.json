{
  "name": "proto",
  "version": "1.0.0",
  "scripts": {
    "lint": "clang-format -n --Werror *.proto",
    "lint:fix": "clang-format -i *.proto",
    "precommit": "exit 0"
  },
  "devDependencies": {
    "ts-proto": "^1.112.1"
  }
}
